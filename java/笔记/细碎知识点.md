1. 用空格可以同时接收两组数据  

2. 用变量初始化数组合法，变量必须在使用之前被赋值  

3. 方法的形参名可以和属性名相同，但要用this指针区分  

4. 方法不知返回类型时，在方法后加.var,自动帮你生成变量  

5. ctrl + 鼠标 查看类、方法的具体细节  

6. ctrl + alt + l 格式化代码

7. ctrl + alt + t 选择模版，可while,if,直接抛出异常...

8. idea右键菜单中的图表可以查看类图

9. ctrl + alt + 左右方向键  可以便捷地实现源码访问的跟踪

10. <mark>final修饰数组时，其指向的地址不能修改，但数组元素的值可以修改</mark>-->引用数据类型，类似类

11. ctrl + p 调用方法时显示形参

12. 跳出嵌套循环  给while起名字，要符合标识符规范

```java
abc:while(true){
    {
        break abc;
    }
}
```

13. alt + 回车 直接在测试类中创建未创建的函数或实现接口的方法
13. ctrl + alt + M 将选中的代码块抽象成方法
13. ctrl + n 搜索类,文件...
13. ctrl + alt + v  自动为方法生成==返回==的变量名与类型
13. alt + 7 打开类的结构（大纲）-->可检索
13. shift + f6 批量改名
13. Arrays的binarySearch()。若不存在，返回 ==- x - 1== --> - 1是为了区分索引0，设插入点索引为x
13. alt + 鼠标左键  竖着选中
13. 在实体类中，基本数据类型建议使用对应的包装类-->避免默认值
